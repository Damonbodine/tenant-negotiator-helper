
// Placeholder for the keyManager
// This will be moved from the original keyManager.ts file
export const saveApiKey = async (keyName: string, value: string) => {};
export const getApiKey = async (keyName: string) => null;
export const hasApiKey = async (keyName: string) => false;
